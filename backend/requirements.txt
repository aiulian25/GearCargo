# GearCargo Backend Dependencies
#
# Versioning policy: `~=MAJOR.MINOR` (PEP 440 compatible release).
# This allows pip to pull in patch-level and minor security fixes
# automatically on each rebuild, while preventing major-version
# API-breaking upgrades.  Pin to ==EXACT only when a dependency has
# known breaking changes in its minor series (currently: none).
#
# To update: pip install -U <pkg> then rebuild image and run tests.

# Web Framework
Flask~=3.0
gunicorn~=22.0
Werkzeug~=3.0

# Database
Flask-SQLAlchemy~=3.1
SQLAlchemy~=2.0
psycopg2-binary~=2.9
Flask-Migrate~=4.0
alembic~=1.13

# Session & Caching
Flask-Session~=0.8
redis~=5.0

# Background task queue (opt-in: TASK_QUEUE_BACKEND=rq). Uses the existing Redis.
# The default 'thread' backend does not require this package at runtime, but it
# is bundled so enabling RQ needs no image rebuild.
rq~=1.16

# Authentication & Security
Flask-Login~=0.6
Flask-Bcrypt~=1.0
PyJWT~=2.12
PyOTP~=2.9
qrcode[pil]~=7.4
Flask-Talisman~=1.1
Flask-Limiter~=3.5
Flask-Cors~=6.0
Flask-WTF~=1.2

# Compression
Flask-Compress~=1.14

# Email
Flask-Mail~=0.9

# Push Notifications
pywebpush~=1.14

# Validation
marshmallow~=3.20
email-validator~=2.1

# OCR
pytesseract~=0.3
Pillow~=12.0
pillow-heif~=1.0    # decode/transcode iPhone HEIC/HEIF receipt photos (F9); wheels vendor libheif
numpy~=2.0          # local-illumination normalisation in the OCR preprocessing pipeline

# PDF Generation
reportlab~=4.0
pypdf~=4.0
weasyprint~=68.0

# Scheduling
APScheduler~=3.10
Flask-APScheduler~=1.13

# HTTP Client
requests~=2.31
httpx~=0.25

# Date/Time
python-dateutil~=2.8
pytz~=2023.3

# Calendar
icalendar~=5.0
caldav~=1.3
vobject~=0.9

# Environment
python-dotenv~=1.0

# Geolocation (S10 fix — replaces external ip-api.com HTTP call)
# Reads the free MaxMind GeoLite2-City MMDB file configured via GEOIP_DB_PATH.
# No external network requests are made during login.
geoip2~=4.8

# Utilities
uuid~=1.30
shortuuid~=1.0

# Data encryption
cryptography~=49.0
